//! Parameter management: the runtime control-profile registry, the
//! dual-slot flash store, the boot/save state service, and the motor
//! commissioning record.

pub mod motor_param;
pub mod parameter_service;
pub mod parameter_store;
pub mod parameters;
